This Dockerfile builds a base ubuntu:latest image with "supervisor" installed.

It is meant to be the base image for containers that will run 1+ processes in a detached session.

Image in Dockerhub:

q4p14/supervisor:latest
